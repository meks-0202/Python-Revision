number = int(input("Enter a number: "))
len = len(str(number))
#print (number, len)
sum =0 
temp = number
while temp > 0:
    digit = temp % 10
    sum += digit ** len
    temp = temp // 10

if sum == number:
    print(True)
else:
    print(False)
